# Intro
a = [1, 2, 3]
# b = [4, 5, 6]
# c = a + b
# print(c)
# a += [4]  # add a new list

# a.append(5)     # add a new value
# a = a.remove(1)       # remove a value
print(a)
# remove and return by default last item
# a.pop()
# we can remove by index too
# a.pop(0)
